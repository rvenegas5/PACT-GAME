import { createTheme } from "@mui/material/styles";
import { colors } from "@mui/material";

const Theme = createTheme({
  palette: {
    primary: {
      main: "#A46D51"
    }
  }
});

export { Theme };
